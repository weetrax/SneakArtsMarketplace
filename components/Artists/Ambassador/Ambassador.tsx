import * as React from "react";
import PropTypes from "prop-types";

type AmbassadorProps = {
  //
};

const Ambassador: React.FC<AmbassadorProps> = () => {
  return <div>Ambassador</div>;
};

Ambassador.propTypes = {
  //
};

export default Ambassador;
